# Tenant isolation test config (pool mode + tenant limits)
port = 16432
upstream_host = 127.0.0.1
upstream_port = 15432
context_variables = app.current_tenant_id
superuser_bypass = postgres
log_level = debug
pool_mode = session
pool_size = 10
pool_password = testpass
upstream_password = testpass
tenant_deny = blocked_tenant
tenant_max_connections = 2
tenant_query_timeout = 3
