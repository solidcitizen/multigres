# Resolver mode test config
port = 16432
upstream_host = 127.0.0.1
upstream_port = 15432
context_variables = app.user_id
superuser_bypass = postgres
log_level = debug
resolvers = tests/resolvers-test.toml
